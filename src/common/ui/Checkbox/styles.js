import styled from "@emotion/styled";

const Container = styled.div`
  margin-bottom: 8px;
  label {
    display: flex;
    cursor: pointer;
  }

  input {
    margin-right: 8px;
    margin-top: 4px;
  }
`;

export { Container };
