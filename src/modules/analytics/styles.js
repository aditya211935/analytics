import styled from "@emotion/styled";

const Heading = styled.h1`
  color: ${({ theme }) => theme.palette.black["200"]};
  font-size: 24px;
  font-weight: 700;
  margin-bottom: 24px;
`;

export { Heading };
