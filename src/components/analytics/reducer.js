import {} from "./action-types";

const initialState = {};

function analyticsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export { analyticsReducer };
